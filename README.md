# ROS 2 Motor Control with L298N
This project controls a DC motor with an L298N motor driver via an Arduino Uno using ROS 2 Humble.

## Usage
- `driver_node`: Communicates with the Arduino via the serial port.
- `control_node`: Reads keyboard inputs (W, S, X).
